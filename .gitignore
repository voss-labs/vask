# binaries
/bin/
*.exe
*.test
*.prof

# local data
*.db
*.db-journal
*.db-wal
*.db-shm
data/
backups/

# host keys (generated locally; do not commit)
host_ed25519
host_ed25519.pub
*.pem

# editor / os
.DS_Store
.idea/
.vscode/
*.swp

# go
go.work
go.work.sum

# secrets — deploy scripts read from .env / .env.local
# .env.example is the only env file that's safe to track
.env
.env.local
.env.*
!.env.example

# operator-only one-shot scripts (kept locally, never committed)
deploy/reset-vm.sh
